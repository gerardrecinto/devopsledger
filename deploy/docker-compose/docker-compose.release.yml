version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-devopsledger}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-devopsledger}
      POSTGRES_DB: ${POSTGRES_DB:-devopsledger}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-devopsledger}"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  api:
    image: ghcr.io/gerardrecinto/devopsledger/api:${DEVOPSLEDGER_VERSION:-1.1.0}
    restart: unless-stopped
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-devopsledger}:${POSTGRES_PASSWORD:-devopsledger}@postgres:5432/${POSTGRES_DB:-devopsledger}
      REDIS_URL: redis://redis:6379
      LOG_LEVEL: ${LOG_LEVEL:-info}
      ENVIRONMENT: ${ENVIRONMENT:-production}
      ENABLE_DOCS: ${ENABLE_DOCS:-false}
      OFFLINE_MODE: ${OFFLINE_MODE:-true}
      TELEMETRY_ENABLED: ${TELEMETRY_ENABLED:-false}
      RISK_RULES_PATH: ${RISK_RULES_PATH:-}
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  web:
    image: ghcr.io/gerardrecinto/devopsledger/web:${DEVOPSLEDGER_VERSION:-1.1.0}
    restart: unless-stopped
    environment:
      API_URL: ${API_URL:-http://api:8000}
    ports:
      - "3000:3000"
    depends_on:
      - api

  worker:
    image: ghcr.io/gerardrecinto/devopsledger/worker:${DEVOPSLEDGER_VERSION:-1.1.0}
    restart: unless-stopped
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-devopsledger}:${POSTGRES_PASSWORD:-devopsledger}@postgres:5432/${POSTGRES_DB:-devopsledger}
      REDIS_URL: redis://redis:6379
      LOG_LEVEL: ${LOG_LEVEL:-info}
      OFFLINE_MODE: ${OFFLINE_MODE:-true}
      TELEMETRY_ENABLED: ${TELEMETRY_ENABLED:-false}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

volumes:
  postgres_data:
  redis_data:
