apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "devopsledger.fullname" . }}-worker
spec:
  replicas: {{ .Values.worker.replicas }}
  selector:
    matchLabels:
      app: {{ include "devopsledger.fullname" . }}-worker
  template:
    metadata:
      labels:
        app: {{ include "devopsledger.fullname" . }}-worker
    spec:
      containers:
        - name: worker
          image: "{{ .Values.image.worker.repository }}:{{ .Values.image.worker.tag }}"
          imagePullPolicy: {{ .Values.image.worker.pullPolicy }}
          envFrom:
            - configMapRef:
                name: {{ include "devopsledger.fullname" . }}-config
            - secretRef:
                name: {{ include "devopsledger.fullname" . }}-secret
